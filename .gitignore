node_modules/
# Only ignore a root-level dist. The theme's built assets
# (wordpress/solar365/dist) are committed so the theme is deployable without a
# local build step.
/dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
.claude/

# WordPress theme zip artifact
wordpress/solar365-theme.zip
